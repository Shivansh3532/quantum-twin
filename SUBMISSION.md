# Quantum Twin — Developer Tools submission

**Differentiator:** Quantum Twin turns post-quantum migration from one generated patch into a constraint-driven tournament whose winner is selected by external deterministic evidence.

Hosted demo: https://quantum-twin.vercel.app
Repository: https://github.com/Shivansh3532/quantum-twin

Quantum Twin recursively discovers supported native RSA signing, verification, and encryption boundaries across one to twelve local Node repositories, synthesizes a reviewable system contract, and gives identical frozen evidence to two isolated `gpt-5.6-sol` Codex SDK builders. Direct Cutover removes controlled RSA paths; Compatibility Bridge retains only declared frozen-consumer continuity. Deterministic TypeScript executes the approved system workflow twice, disqualifies failed candidates, selects only among eligible results, and returns **NO SAFE WINNER** when none qualify.

Two genuine CommonJS/npm release-fixture reports show the core decision:

- Compatibility required: run `2026-07-19T18-04-56-297Z`; Direct fails compatibility twice; Bridge selected.
- Compatibility disabled: run `2026-07-19T18-08-18-178Z`; both qualify; Direct selected because it retains fewer RSA signatures.

Fresh coordinated multi-repository evidence is published as [`evidence/system-demo-run.json`](evidence/system-demo-run.json). The real run `2026-07-20T03-19-58-791Z` changed both producer and consumer repositories; Direct failed the frozen-client gate and Bridge passed both independent system evaluations plus rollback. Source report SHA-256: `4b488bcc8eb69d8d149390e0a9cc82b3673edf2ba54146dacd6525efb0a9af16`; public presentation SHA-256: `aa2860bf82ddc95aa59220a368f8d44738cef8521504975ee57eb87251fd1c73`.

`@openai/codex-sdk@0.144.6` created both candidate worktrees with exact `gpt-5.6-sol`. GPT-5.6 performs schema-validated classification and explains immutable evidence after selection. Deterministic post-validation rejects GPT files or operations that contradict scanner evidence. GPT cannot control gates, measurements, eligibility, or result. Direct Responses API was not used because `OPENAI_API_KEY` was unavailable.

NIST standardized the first post-quantum cryptography algorithms in 2024 and urges migration. NCCoE separates discovery from interoperability testing, while CISA/NSA/NIST urge early planning and inventory. Quantum Twin focuses on the next developer-level step: compare concrete implementations against frozen compatibility evidence.

Automatic migration scope remains Node.js 24 TypeScript/JavaScript with native `node:crypto` RSA signing/verification to ML-DSA-65. ML-KEM-768 KEM-DEM, frameworks, workspaces, and multi-repository execution remain explicitly EXPERIMENTAL until their full cross-platform proof rows are complete. TLS/X.509, JWT/JOSE, KMS/HSM, third-party crypto, non-Node runtimes, and Docker execution are discovery-only or experimental as shown in `support-matrix.json`. No private credentials, automatic PRs, hostile-code sandbox, formal verification, certification, security guarantee, or deployment approval.

Majority-core `/feedback` Session ID: `019f774d-0364-76a3-bd72-cb806fe0109a`.
