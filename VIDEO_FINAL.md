# Final video — 2:35 target

Recommended filename: `quantum-twin-build-week-judge-demo-2m35s.mp4`

## 0:00–0:15 — problem and differentiator

Show hosted hero, SCAN / COMPETE / PROVE flow, primary actions, and both genuine-run badges.

Audio: “A crypto scanner finds legacy algorithms. One generated patch still does not prove compatibility. Quantum Twin turns post-quantum migration from one generated patch into a constraint-driven tournament whose winner is selected by external deterministic evidence.”

## 0:15–0:35 — current generalized fixture and scanner

Show `fixtures/release-cli/lib/release-signer.cjs`, then scanner section.

Audio: “This project-owned CommonJS npm release fixture signs and verifies with RSA. The recursive scanner records both operations, exact file and lines, import form, algorithm evidence, confidence, source identity, package manager, and versioned contract.”

## 0:35–0:55 — Codex and GPT-5.6

Show candidate thread IDs and immutable contract.

Audio: “Two isolated Codex SDK builders use exact GPT-5.6-sol and the same source commit, writable boundaries, ML-DSA-65 target, context, commands, and external harness. Only strategy differs: Direct Cutover versus Compatibility Bridge. GPT-5.6 classifies scanner evidence and later explains results; deterministic TypeScript rejects contradictions and controls selection.”

## 0:55–1:30 — compatibility required

Show Bridge result and gate matrix; point to Direct compatibility failures in both passes.

Audio: “With legacy compatibility required, Direct produces valid ML-DSA but fails the frozen RSA consumer in evaluator passes one and two, so it is ineligible. Bridge preserves declared RSA continuity, passes every gate twice, and is selected. GPT cannot override a failed gate.”

## 1:30–1:50 — compatibility disabled

Use selector. Show aria-live confirmation and Direct result.

Audio: “Now I change one declared requirement. Same repository and strategies; compatibility is disabled. Both qualify, and deterministic policy selects Direct because it retains zero RSA signatures.”

## 1:50–2:15 — evidence depth

Expand Direct diff, commands, measurements, provenance, and explanation.

Audio: “Each genuine report preserves Codex threads, candidate commits, full diffs and hashes, command exits and timings, two evaluator passes, compatibility-harness hash, Node and platform metadata, measurements, limitations, and final report hash. The standalone verifier detects modified evidence.”

## 2:15–2:35 — local path, refusal principle, boundaries, and close

Show Run on your repository, NO SAFE WINNER, implemented/discovery-only/verified scope, and URLs.

Audio: “Automatic migration stays narrow: trusted local Node repositories using native RSA and declared harnesses. TLS, KMS, HSM, third-party crypto, and other languages are discovery-only. No eligible candidate means NO SAFE WINNER. Hosted mode is read-only; local live mode uses authenticated Codex without an OpenAI API key. Repository and demo links are on screen.”

Recording checks: clear audio; say “Codex” and “GPT-5.6”; final duration below three minutes; public YouTube visibility; no music, external repository, third-party logos, screenshots, or unrelated tabs.
