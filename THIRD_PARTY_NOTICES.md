# Third-party notices

Quantum Twin source, fixtures, tests, UI, documentation, and sample-report selection are project-owned and distributed under [MIT](LICENSE). Public sample reports are genuine outputs created from the project-owned `fixtures/release-cli` fixture.

Pinned direct dependencies:

- `@openai/codex-sdk` and `@openai/codex`: Apache-2.0.
- Next.js, React, React DOM, Zod, Tailwind CSS, TypeScript, tsx, and Vitest: permissive open-source licenses recorded in `pnpm-lock.yaml` and their package metadata.

Relevant transitive assets in the pinned installation include MIT, Apache-2.0, ISC, BSD-3-Clause, 0BSD, and CC-BY-4.0 components. Sharp platform packages identify Apache-2.0 and LGPL-3.0-or-later components. Inspect the exact installed set with:

```bash
pnpm licenses list --prod
```

The hosted UI uses project-owned CSS, text, and an original QT SVG app icon with system fonts. Repository contains no third-party images, icons, logos, stock footage, music, or screenshots. Competition video should show only Quantum Twin UI, repository-owned fixture code, terminal output, repository files, and evidence reports.
